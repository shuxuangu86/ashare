# ADR-0002：实盘能力默认拒绝

- 状态：Accepted
- 日期：2026-07-16

## 决策

系统配置的默认模式是 `BACKTEST`。MVP 不开放 `LIVE_FULL`；`LIVE` 配置必须同时
显式启用实盘并要求人工确认。真实券商配置中的提交能力默认关闭。

## 原因

安全不能依赖操作人员记得关闭某个开关。缺少配置、配置拼写错误或服务重启都应回到
拒绝交易的状态。

## 后续门控

后续执行层还必须检查：当日数据发布、账户核对、交易前风控、行情新鲜度、Kill
Switch、券商连接和一次性人工批准。任何一项失败都不得产生新订单。

