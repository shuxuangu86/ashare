# Research archive

Store experiment notes, failed hypotheses, frozen configurations, and links to registered artifacts
here. Large generated datasets and model binaries stay outside Git and are referenced by checksum.

