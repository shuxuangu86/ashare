# Notebooks

Notebooks are disposable research clients. They may call tested functions from `src/aquant`, but
must not contain the only copy of a factor, strategy, data transformation, or execution rule.

